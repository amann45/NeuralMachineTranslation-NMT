#### Why checkpoints?
Checkpoints were used to save the best-performing model during training. This ensures that the model with the lowest validation loss can be restored later for testing and prediction, even if subsequent training epochs result in slightly poorer performance.
